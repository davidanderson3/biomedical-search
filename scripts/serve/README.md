# Serving Script Index

Index-only category. Executable entrypoints remain at `../<script>.py` and
`../<script>.sh` so existing command references keep working.

- `../search_quality_server.py` - local search API and browser UI server.
- `../start_search_quality_server.sh` - shell launcher for the search-quality
  server.
